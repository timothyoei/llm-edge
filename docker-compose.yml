version: "3"
services:
  llm_edge:
    build:
      context: .
    volumes:
      - .:/playground
    networks:
      - llm_edge_playground
    ports:
      - "5000:5000"
      - "8000:8000"
    tty: true
networks:
  llm_edge_playground:
    driver: bridge